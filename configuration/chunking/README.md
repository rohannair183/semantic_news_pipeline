# Chunking Configuration

This folder contains semantic chunking configuration.

- `chunking.yaml`: controls input/output parquet paths, text/id/profile fallback columns, and semantic splitting parameters (min/max chars, overlap, similarity threshold, sentence splitter mode).
